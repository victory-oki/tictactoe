import styled from "styled-components";
import { white } from "../../theme";

export const SoundPlayerStyled = styled.div`
    .volume-icon{
        font-size: 2rem;
        cursor: pointer;
        color: ${white[100]};
    }
`
